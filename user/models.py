from django.db import models
from django.contrib.auth.models import User
# Create your models here.

class MainUser(User):
    pass
    # password = models.CharField(min(8))
    # passwordConfirmation = models
